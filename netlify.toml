[build]
  base    = "ui/mad-architecht-command-center"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Public command center
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
